syntax = "proto3";

package geecachepb;

message GetRequest {
    string group = 1;
    string key = 2;
}

message GetResponse {
    bytes value = 1;
}

service GroupCache {
    rpc Get(GetRequest) returns (GetResponse) {

    };
}